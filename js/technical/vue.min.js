// Minimal Vue.js stub for development purposes
window.Vue = function Vue(options) {
  if (typeof options === 'string') {
    this.$el = document.querySelector(options);
    return this;
  }
  
  this.$data = options.data || {};
  this.$methods = options.methods || {};
  this.$computed = options.computed || {};
  this.$watch = options.watch || {};
  this.$props = options.props || {};
  
  if (options.el) {
    this.$el = typeof options.el === 'string' ? document.querySelector(options.el) : options.el;
  }
  
  return this;
};

Vue.component = function() {};
Vue.config = { devtools: false };
Vue.nextTick = function(fn) { setTimeout(fn, 0); };
Vue.extend = function(options) { return Vue; };
Vue.use = function() { return Vue; };
Vue.mixin = function() { return Vue; };
Vue.filter = function() { return Vue; };
Vue.directive = function() { return Vue; };
Vue.set = function(obj, key, value) { obj[key] = value; return value; };
Vue.delete = function(obj, key) { delete obj[key]; };
Vue.observable = function(obj) { return obj; };
Vue.version = '2.7.16';
Vue.prototype = {};

window.Vue = Vue;